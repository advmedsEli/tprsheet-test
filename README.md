# tprsheet

```
<Tprsheet :fields="fields" :items="items" />
```

`items`: array
必要: date

`fields` Object[]
* `key` String
* `label` String
* `single` Boolean
* `children` String[]
* `chart` Boolean
* `key` String